/**
 * Client-safe bridge for the shared browser-notification helper (same import
 * boundary the workflows plugin uses) — behaviour is the host's.
 */
export { sendBrowserNotification } from '../../../src/lib/browser-notify'
