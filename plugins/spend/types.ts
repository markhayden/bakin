/** Spend plugin — shared types. The policy/billing/pricing shapes move here in the ownership cutover (T2.7). */
export type SpendTab = 'overview' | 'limits'
export type SpendWindow = '24h' | '7d' | '30d' | 'all'
