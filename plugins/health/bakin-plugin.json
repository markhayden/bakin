{
  "id": "health",
  "name": "Health",
  "version": "1.0.0",
  "bakin": ">=1.0.0",
  "description": "System health dashboard — MCP stats, diagnostics, and uptime",
  "entry": {
    "server": "index.ts"
  },
  "contentFiles": [],
  "secrets": [],
  "tests": "tests/",
  "dependencies": [],
  "permissions": [
    "storage.read",
    "runtime.read",
    "runtime.agents",
    "runtime.channels",
    "runtime.skills",
    "search.read"
  ],
  "contributes": {
    "apiRoutes": [
      {
        "method": "GET",
        "path": "/checks",
        "summary": "List registered plugin health checks (metadata only; does not execute them)."
      },
      {
        "method": "GET",
        "path": "/doctor",
        "summary": "Run or read cached doctor diagnostics"
      },
      {
        "method": "GET",
        "path": "/registry",
        "summary": "Inspect registered plugin and runtime surfaces"
      },
      {
        "method": "GET",
        "path": "/search-status",
        "summary": "Read search indexing health and adapter status"
      },
      {
        "method": "GET",
        "path": "/summary",
        "summary": "Get a compact system health summary"
      },
      {
        "method": "GET",
        "path": "/usage",
        "summary": "Get current token and cost usage metrics"
      },
      {
        "method": "GET",
        "path": "/usage-feed",
        "summary": "Stream usage updates for the health dashboard"
      }
    ],
    "execTools": [
      {
        "name": "bakin_exec_health_doctor",
        "summary": "Run system diagnostics (agent roster, skill sync, runtime, taskboard, assets, etc",
        "description": "Run system diagnostics (agent roster, skill sync, runtime, taskboard, assets, etc.). Returns detailed check results. Use fresh=true to force a full re-check instead of returning cached results."
      },
      {
        "name": "bakin_exec_health_status",
        "summary": "Get a quick system health summary — uptime, memory, active MCP sessions, and doctor error/warning counts",
        "description": "Get a quick system health summary — uptime, memory, active MCP sessions, and doctor error/warning counts. Useful for checking system state before starting work."
      }
    ],
    "cliCommands": [
      {
        "name": "status",
        "usage": "bakin status",
        "summary": "Show dispatch and server status."
      },
      {
        "name": "doctor",
        "usage": "bakin doctor [--full] [--notify-agent] [--fix|--delegate] [--yes]",
        "summary": "Run health checks, apply explicit safe fixes, or create a delegated repair task."
      }
    ],
    "docs": {
      "slug": "using/health"
    }
  }
}
