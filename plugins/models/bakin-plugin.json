{
  "id": "models",
  "name": "Models",
  "version": "3.0.0",
  "bakin": ">=1.0.0",
  "description": "Which model runs what \u2014 a Simple two-lane plan (agent model + background chores) or every per-agent and per-job control, one validated write path, and a recommender that proposes but never applies",
  "contentFiles": [],
  "secrets": [
    {
      "name": "ANTHROPIC_API_KEY",
      "description": "Anthropic API key used to fetch provider model metadata when available.",
      "required": false
    },
    {
      "name": "OPENCLAW_GATEWAY_TOKEN",
      "description": "OpenClaw gateway token used when model operations are routed through the runtime gateway.",
      "required": false
    }
  ],
  "dependencies": [
    "team"
  ],
  "permissions": [
    "storage.read",
    "storage.write",
    "events.emit",
    "runtime.read",
    "runtime.agents",
    "runtime.models"
  ],
  "contributes": {
    "apiRoutes": [
      {
        "method": "GET",
        "path": "/aliases",
        "summary": "List model aliases"
      },
      {
        "method": "GET",
        "path": "/available",
        "summary": "Bypass cache and fetch the model list fresh from the runtime adapter"
      },
      {
        "method": "GET",
        "path": "/config",
        "summary": "Get effective agent model configuration"
      },
      {
        "method": "GET",
        "path": "/routing",
        "summary": "Get the per-turn model/thinking routing policy"
      },
      {
        "method": "POST",
        "path": "/refresh",
        "summary": "Bypass cache and fetch the model list fresh from the runtime adapter"
      },
      {
        "method": "POST",
        "path": "/runtime/restart",
        "summary": "List available AI models with tier classification (budget/standard/premium). Use this to discover what models are available for assignment."
      },
      {
        "method": "GET",
        "path": "/runtime/status",
        "summary": "Check if runtime config is out of sync (needs restart)"
      },
      {
        "method": "GET",
        "path": "/aliases/recommended",
        "summary": "Recommended alias set (data for the one write path)"
      },
      {
        "method": "GET",
        "path": "/selections",
        "summary": "Every persisted model selection with eligibility, proposals and pending writes"
      },
      {
        "method": "POST",
        "path": "/selections",
        "summary": "Apply model-selection changes (the ONE write path)"
      },
      {
        "method": "GET",
        "path": "/holds",
        "summary": "Todo tasks held because their effective model cannot run"
      },
      {
        "method": "GET",
        "path": "/plan",
        "summary": "The recommended two-lane model plan (read-only)"
      }
    ],
    "execTools": [
      {
        "name": "bakin_exec_models_get_config",
        "summary": "Get model configuration for all agents or a specific agent",
        "description": "Get model configuration for all agents or a specific agent. Shows effective model (own override or default), subagent model, and system defaults."
      },
      {
        "name": "bakin_exec_models_list",
        "summary": "List available AI models with tier classification (budget/standard/premium)",
        "description": "List available AI models with tier classification (budget/standard/premium). Use this to discover what models are available for assignment."
      }
    ],
    "docs": {
      "slug": "using/models"
    },
    "nav": [
      {
        "id": "models",
        "label": "Models",
        "icon": "Cpu",
        "href": "/models",
        "order": 30,
        "section": "operations"
      }
    ],
    "slots": [
      "page:/models"
    ]
  }
}
