# Bakin

Bakin is a self-hosted multi-agent orchestration platform. It gives a single user a real-time dashboard into what their AI agents are doing — tasks, projects, workflows, assets, schedules — all powered by markdown files on the filesystem, pushed to the browser via SSE.

Runs on a Mac mini, accessed via Tailscale. No SaaS dependencies.

## Architecture

- **Runtime:** Bun (>=1.2.0). No Node.js, no pnpm, no Next.js. Bun is the runtime, the bundler, and the package manager.
- **Server:** `server.ts` uses Node's `http.createServer` hosted under Bun (same shape as pre-migration; Bun runs the node-compat path). Port 3737. MCP server, SSE broadcaster, Chokidar file watcher, dispatch loop all unchanged from pre-#147.
- **API handlers:** Web Fetch-style `(req: Request, url: URL) => Promise<Response>` functions at `packages/host/src/api/**/*.ts`. Dispatched from the request router (`src/core/server/request-handler.ts`, a factory `server.ts` passes to `http.createServer`) via `dispatchWebHandler` (see `packages/host/src/api/_adapter.ts`). The plugin catch-all at `packages/host/src/api/plugins/[pluginId]/[[...path]].ts` matches against the live `pluginRegistry`.
- **Client shell:** `packages/host/src/` — React 19 + TanStack Router (code-based routes in `packages/host/src/routes/`). Built via `Bun.build()` to `packages/host/dist/main.js`. Externalizes `react`, `react-dom`, `react-dom/client`, `react/jsx-runtime`, `@makinbakin/sdk`, and every `@makinbakin/sdk/*` sub-path.
- **Import map + vendor bundles:** `packages/host/public/index.html` emits a `<script type="importmap">` mapping externalized specifiers to `/vendor/*.js` bundles produced by `scripts/build-vendors.ts`. One React instance, one SDK instance, shared by shell + every plugin. The nine SDK sub-paths are built in a single `bun build --splitting` invocation — shared code lives once in content-hashed `sdk-shared-*.js` chunks (relative imports, no import-map entries; #422).
- **Core plugins:** 13 plugins under `plugins/<id>/`, each built by `scripts/build-plugins.ts` into `plugins/<id>/dist/` — browser assets only (`client.js` + optional `client.css`; no server bundle — core server code is statically imported via `src/lib/plugin-static-imports.ts`, #421) with externals matching the shell. Each plugin's `client.tsx` calls `registerPlugin({ id, navItems, slots })` from `@makinbakin/sdk` as a module side effect.
- **Runtime plugin loader:** `packages/host/src/plugin-host/PluginHost.tsx` wraps the shell. On mount it fetches `/api/plugins/manifest`, then dynamic-imports each plugin's `clientEntry` URL (`/api/plugins/<id>/assets/client.js`). Each plugin module runs `registerPlugin` during load, populating the browser-global nav + slot registry. `AppSidebar` reads nav from `getAllNavItems()`.
- **User plugins:** Installed into `~/.bakin/plugins/<id>/` via `bakin plugins install <path|github:user/repo>` — the installer copies source, runs `buildUserPlugin()` (in-binary Bun.build + optional `bun install` when the plugin declares deps), and the next server restart picks it up. User plugins structurally match core plugins; the loader doesn't know which bucket a plugin came from.
- **Plugin lifecycle:** `bakin plugins {list [--check], install [--yes], export, import, upgrade [--yes], remove}` — install/upgrade/remove with consent prompts (#142), reproducible plugin-set import/export (#168), per-plugin teardown sweep + tarball backup (#119), and an install ledger at `~/.bakin/plugins/lock.json` (mirrors agent-packages lockfile pattern). Core plugins refuse `upgrade` and `remove` via `isCorePlugin()`. Deep reference: `.claude/knowledge/plugin-lifecycle.md`.
- **Storage:** Markdown files, JSON sidecars, and JSONL logs in `~/.bakin/`. Tasks live in the Bakin task-store under `~/.bakin/tasks/`.
- **Real-time:** Server-Sent Events (SSE) push updates to all connected browsers.
- **Agents:** Managed through `AppServices.runtime` / `ctx.runtime`. Two runtime implementations: OpenClaw (`packages/adapter-openclaw/`, gateway/MCP-based) and Pi (`packages/adapter-pi/`, in-process SDK; deep reference `.claude/knowledge/pi-adapter.md`). Selected by `settings.runtime.adapter`.
- **Search:** `AppServices.search` / `ctx.search` for full-text + semantic search behind an adapter-neutral capability contract (D17: antfly is the DEFAULT adapter, not the design — an architecture test bans antfly identifiers upstream of the adapter). Antfly (v0.2 Zig, pinned + SHA256-verified direct download) runs as an **OS-supervised service** (launchd/systemd user unit; strict-child fallback in Docker/CI) on `127.0.0.1:3738`, data under `~/.bakin/antfly/` — Bakin is a raw-fetch HTTP client (`packages/adapter-antfly/`; there is NO `@antfly/sdk` dependency). Writes journal through a durable SQLite **outbox** (`~/.bakin/search.db`) and land via a drain pump — engine down means rows wait, never lost. Tables are **blue/green versioned** (`{logical}_v{schemaVersion}_{fp8}`): schema/model changes migrate in the background with queries pinned to the old table until convergence; **boot performs zero engine calls** when state matches. Migration mechanics (redesigned 2026-07-21 after the rc.21 incident): only the embed-heavy **backfill** serializes process-wide — converge-waits run per-table off-chain; convergence is **progress-aware** (frozen counts park in ~60s, progressing greens get up to 30 min; failed legs park immediately; a failed stats read is never flip evidence); parked migrations **self-heal via the migration pump** (5-min tick, attempt-capped, resumes the RECORDED `migrating_to`/`migrating_fp` target — never recomputed); `bakin reindex` is **repair-by-default** (resume parked, regenerate engine-missing, migrate drifted, skip healthy; `--force` mints fresh generations) and single-flights overlapping passes; `bakin install search` re-provisions the service unit unconditionally and treats an engine **version change as a rebuild event** (derived data dir cleared → repair reindex regenerates). Every search request runs under a **query budget** (`settings.search.settings.search.queryBudgetMs`, default 2s): per-table cooperative deadlines (rc.18 `timeout_ms` → 504), honest degrade to keyword-only or omission with `meta.partial`/`meta.tables[]` labels — never a silent stall (D11). Health surfaces carry per-table **freshness + numeric backlog** (`lastIndexedAt`/`journalPending`/leg `pending`), and the doctor's `search-spin` watchdog catches zero-progress backfills (the #319 CPU-burn signature) with a one-click blue/green rebuild repair. Surviving upstream limitations are pinned by tests that fail when upstream fixes them (`tests/integration/antfly/workaround-regressions.test.ts`; open: the empty/runtime-less-leg flags lie + the #319 idle-accounting family + filter_query match_phrase 400s; #317 fixed at rc.18. rc.19–rc.21 were evaluated 2026-07-22 and REVERTED: concurrent embed-bearing writes crash the engine, it exits mid-create on empty data, and in-place upgrades migrate table files one-way — crash dossier filed upstream; re-evaluate future releases with the reproduction ladder BEFORE repinning. rc.18 uses the `swarm` server subcommand; rc.19+ renamed it `standalone`).
- **Adapter Boundary:** Runtime/search provider details stay behind adapter packages and the factories in `src/core/*-adapter-factory.ts`. Bakin owns UI data, task metadata, audit, assets, workflows, plugin settings/data, heartbeats, and **the scheduling of Bakin tasks** (the schedule plugin runs its own tick scheduler — see below). Runtime providers own agent identity, soul, rules, tools, models, workspace data, channels, memory, and **the cron jobs the runtime/agents create for themselves** (Bakin surfaces those read-only). Deep references: `.claude/knowledge/adapter-architecture.md`, `.claude/knowledge/bakin-owned-scheduler.md`. Audit skill: `.claude/skills/check-adapter-boundary.md`.
- **Agent Packages:** A second primitive distinct from plugins. Plugins ship code (routes, UI, MCP tools); agent packages ship **content** — identity (SOUL/IDENTITY/AGENTS/TOOLS), runtime skills, workflows, and lesson files — that personifies an agent in the active runtime and gives it domain perspective. Manifested as `bakin-package.json` with `kind: "agent" | "skill-pack" | "workflow-pack" | "lesson-pack"`. Two states per agent: `unmanaged`, `managed` (adopt is an install-time action, not a state). All Bakin/package-managed workspace content lives in ONE composed `bakin:managed` block per file, layered from global/role/team context + package template + lessons; `bakin agents sync` is the single maintenance verb. Deep references: `.claude/knowledge/agent-packages.md`, `.claude/knowledge/layered-context.md`.

## Build, Dev, CLI

- **Binary:** `bun run build` chains vendors → plugins → host shell → `bun build --compile` per platform. Runtime assets are imported via `with { type: 'file' }` in `packages/host/src/api/_embedded-assets-static.ts` (regenerated by `scripts/generate-embedded-assets.ts`) so they're embedded in the single-file binary; core plugin dist files are allowlisted to browser assets (`client.js`/`client.css`) — server bundles are never embedded or served (#421). Release pipeline (`.github/workflows/release.yml`) fires on strict `v*` release tags, signs/notarizes macOS before checksums, publishes `@makinbakin/sdk` through npm trusted publishing, updates `markhayden/homebrew-tap` for stable releases, then publishes the GitHub release. Runbook: `.claude/knowledge/release-pipeline.md`.
- **Dev loop:** `bun run dev` runs `scripts/dev.ts` — chokidar-based watcher coordinator with HMR for plugins (no page reload), `location.reload()` for shell/SDK, CSS link swap, and a fullscreen overlay on build errors. Server-side code is **not** watched; manual restart. `bun run dev:mock` swaps in the Imitation Crab OpenClaw mock. Deep reference: `.claude/knowledge/dev-loop.md`.
- **CLI:** Two entries cooperate. `src/core/cli.ts` is the binary-facing dispatcher (`start`/`stop`/`status`/`version`/`update`/`plugins {…}`/`dev`); unknown commands delegate (lazily, both directions dynamic) to `cli/bakin.ts` — a ~210-line router (npm bin entry) that lazy-imports one module per command group from `src/cli/commands/{tasks,agents,plugins,packages,settings,schedule,budget,search,trash,assets,workflows,runtime,onboarding,doctor,lifecycle,diagnostics,docs,plugin-dispatch}.ts`, with shared plumbing in `src/cli/{http,output,help}.ts` (BAKIN_URL-aware client, print/exit helpers, help rendering). Command modules own their heavy imports (whiskit, import-export, settings) so binary startup stays cheap; extracted command code calls `process.exit` directly (never return codes — the exit-code plumbing and tests depend on it). Plugin-contributed commands (`contributes.cliCommands` in the manifest) dispatch via `plugin-dispatch.ts` and appear in `bakin --help` when the server is reachable. Use `bakin --help` for the full surface.

## Directory Map

The full annotated directory map lives in `.claude/knowledge/repo-architecture.md`. Quick orientation: `packages/{core,sdk,host,adapter-openclaw,adapter-pi,adapter-antfly}` for shared code + SDK + client shell + provider adapters, `plugins/<id>/` for the 13 core plugins, `src/core/` for server-side modules with side effects, `src/lib/` for client+server-safe shared code, `scripts/` for build infrastructure, `cli/` for the HTTP-client CLI router (command modules in `src/cli/commands/`), `dev/imitation-crab/` for the OpenClaw mock. Reference agent packages live in the external `bakin-bits-official` repo, not in-tree.

### Runtime Data Directory (`~/.bakin/`)
Created by `bakin onboard` / `initBakinHome()`. Per-installation state, NOT in the repo.
```
~/.bakin/
  settings.json            — Runtime config (dispatch/watchdog/antfly/bridge settings)
  bakin.db                 — Execution ledger (SQLite, WAL): run claims, cron fires, completions, idempotency
  usage.db                 — Durable usage history (SQLite): per-(session, day, model) token/cost rollups scanned from runtime session transcripts (#359)
  server.lock              — Server singleton lock ({pid, port, startedAt, bootId})
  plugin-settings/         — Per-plugin configuration (id.json)
  plugins/<id>/            — Installed addon plugins (source + generated dist/)
  plugin-data/<id>/        — Installed plugin runtime data
  agents/                  — Per-agent UI data ({id}/avatar.{webp,png,jpg}, avatar-full.png + .installedBy)
  packages/                — Agent-package install state (lock.json + per-kind dirs)
  assets/                  — Versioned assets: store/<YYYY-MM>/<assetId>/ (manifest.json + vN files + exports/); see .claude/knowledge/assets-versioning.md
  antfly/                  — Data dir for Bakin's private antfly instance (127.0.0.1:3738; binary + models live under ~/.antfly)
  health/                  — Incident ack/snooze records (acks.json — the "I know" verb)
  heartbeats/              — Agent status heartbeats (JSON)
  tasks/                   — Bakin-owned task metadata JSON, sharded by created month
  run-workspaces/          — Per-run agent working dirs (<agentId>/<runId>/ + .bakin-run.json sidecar; watcher-excluded, sweep-GC'd)
  brands/                  — Brand records: <id>/{brand.json, guidelines/, lessons/} (#419)
  schedule/                — Cron job state
  workflows/               — Definitions, instances, skills
  team/                    — Contacts, personas
  MEMORY-LOG.md            — Agent memory log
  audit.jsonl              — Append-only audit trail
  logs/server.log          — Rotating server log (10 MB, single backup)
```

## Plugin System

Every plugin has `bakin-plugin.json` (manifest), `package.json` (peer deps on `react` + `@makinbakin/sdk`), `index.ts` (server entry exporting a `BakinPlugin` with `activate(ctx)`), `client.tsx` (calls `registerPlugin({ id, navItems, slots })` as a side effect — exports are not read), `components/`, `types.ts`, and optional `defaults/` for workflows / workflow-skills / runtime-skills.

Core plugins build to `plugins/<id>/dist/`. User plugins build to `~/.bakin/plugins/<id>/dist/` via the in-binary builder (`buildUserPlugin` in `packages/host/src/plugin-host/user-plugin-builder.ts`).

Routes registered as `/api/plugins/{pluginId}/{path}`. Exec tools naming: `bakin_exec_{pluginId}_{action}`.

Deep references: `.claude/knowledge/plugin-system.md`, `.claude/knowledge/workflows-plugin.md`, `docs/src/content/docs/extending/plugins/`.

## Agent Packages

Plugins ship code; agent packages ship content. Every package has `bakin-package.json`, `workspace/` (template SOUL/IDENTITY/AGENTS/TOOLS files seeded once on install), `skills/<name>/`, optional `workflows/*.yaml` + `workflow-skills/*.md`, `lessons/*.md`, and `assets/*`. **Capability packs** are skill-packs with `capability`/`runtimes`/`requires.bins` (pinned sha256 binaries → `~/.bakin/bin`) + enforced `secrets[]` (`secretSlot` → guided key step + boot env injection) — agents gain per-turn powers (web search, …) with Bakin owning the whole install path; taxonomy + lifecycle: `.claude/knowledge/capability-packs.md`.

Lockfile at `~/.bakin/packages/lock.json` is the canonical install ledger; every projected file gets a `.installedBy` sidecar; `.userEdited` sentinels lock projections from being overwritten.

CLI: `bakin agents {install,list,remove,sync,lessons}` and `bakin packages {install,list,remove,sync}` — sync fetches the package source, recomposes managed blocks (global/role/team/package/lessons), re-projects skills/assets, verifies, and writes a receipt (`~/.bakin/packages/receipts/`); `--check` reports without writing; `--reclaim` is the confirmed path past `.userEdited` sentinels. REST: `/api/agent-packages/*` and `/api/packages/*` (top-level, distinct from runtime `/api/agents/*`). Doctor surfaces drift via the `team.agent-sync` check / `bakin check agent-sync` / `bakin install agent-sync`.

Deep references: `.claude/knowledge/agent-packages.md`, `docs/src/content/docs/extending/agents/packages.md`.

## Code Conventions

- **TypeScript strict mode** — no `any` leaking across module boundaries
- **Zod** for validation at system boundaries (API inputs, file parsing, settings)
- **Functional preference** — pure functions over classes where practical
- **Logging:** `const log = createLogger('module')` from `@bakin/core` (re-exported as `src/core/logger`)
- **No empty catch blocks** — always log or rethrow
- **`const` over `let`**, never `var`
- **Files:** `kebab-case.ts` / `kebab-case.tsx`
- **Types/interfaces:** `PascalCase` (e.g., `BakinPlugin`, `PluginContext`)
- **Constants:** `UPPER_SNAKE_CASE` for true constants

### Import Order
```typescript
// 1. Node builtins
import { join } from 'path'
// 2. External packages
import { Database } from 'bun:sqlite'
// 3. SDK (plugin author surface — canonical)
import { registerPlugin, type NavItem } from '@makinbakin/sdk'
import { PluginHeader } from '@makinbakin/sdk/components'
// 4. Internal @/* (inside packages/host and src/ only)
import { createLogger } from '@/core/logger'
// 5. Relative
import { helper } from './utils'
```

Path aliases: `@/*` maps to `./src/*` for server-side code, and core plugins use `@bakin/{plugin}/*` only inside Bakin app code/tests. External plugin authors never use `@/*` or `@bakin/{plugin}/*` — the canonical entry point is `@makinbakin/sdk/*`.

### Commit Conventions
Conventional commits with scope:
- `feat(tasks): add drag-and-drop reordering`
- `fix(schedule): handle timezone edge case`
- `refactor(core): extract settings module`
- `test(workflows): add gate approval test`

## Testing Rules — CRITICAL

**Every test file MUST mock the content-dir resolver AND the OpenClaw home resolver to use temp directories.** Tests that touch storage, assets, tasks, agent packages, or any plugin MUST NOT read from or write to `~/.bakin/` or `~/.openclaw/`. Leaked test data into either production directory has caused real incidents.

Because `src/core/content-dir.ts` is an app-facing facade over `packages/core/src/content-dir.ts`, **mock both**. Any consumer may import from either path (or the `@/core/content-dir` alias), and missing one leaves a leak surface. Same applies to OpenClaw home.

The execution ledger (`~/.bakin/bakin.db`) resolves its path via `getBakinPaths().db` — mocks of `getBakinPaths` **must include the `db` key** or ledger-touching code throws/leaks. Tests exercising the real ledger must call `closeDb()` (from `packages/core/src/storage/db`) before `rmSync`-ing their temp dir: a cached handle over a deleted inode keeps stale rows alive across tests (`SQLITE_IOERR_VNODE` is the symptom). Unit tests that only need the completion gate can mock `src/core/execution-ledger` with an in-memory fake (see `tests/core/task-service.test.ts`).

Required mocks for any test that touches the filesystem:
```typescript
const testDir = join(tmpdir(), `bakin-test-${Date.now()}`)

mock.module('../../src/core/content-dir', () => ({
  getContentDir: () => testDir,
  getBakinPaths: () => { /* return paths under testDir */ },
}))
mock.module('../../packages/core/src/content-dir', () => ({
  getContentDir: () => testDir,
  getBakinPaths: () => { /* return paths under testDir */ },
}))
mock.module('@bakin/adapter-openclaw/home', () => ({
  getOpenClawHome: () => join(testDir, 'openclaw'),
  getOpenClawPath: (...parts) => join(testDir, 'openclaw', ...parts),
  resetOpenClawHome: () => {},
}))
```

When the test imports modules that read `process.env.OPENCLAW_HOME` at module-load time (the openclaw-adapter is the most common one), set the env var BEFORE any imports — `mock.module` calls run after ES-module imports hoist, which is too late for module-top reads:

```typescript
const testDir = pathJoin(tmpdir(), `bakin-test-foo-${Date.now()}-${randomUUID()}`)
process.env.OPENCLAW_HOME = pathJoin(testDir, 'openclaw')
process.env.BAKIN_HOME = testDir
// adapter-pi tests: same pattern with PI_HOME (+ resetPiHome()/resetModelRegistry() after imports)
process.env.PI_HOME = pathJoin(testDir, 'pi')

// imports follow — modules read the env vars on first call
import { ... } from '...'
```

Run the full suite with `bun run test` (CI) or `bun run test:watch` (dev) — both pass `--path-ignore-patterns "dev/**"` so the dockerized-rig's disposable home (`dev/bakin-instances/`, gitignored) doesn't leak stray test files into the run (the pattern is root-anchored — `tests/dev/` still runs; an unanchored `**/dev/**` once hid those tests from every local run). Individual file: `bun test tests/path/to/foo.test.ts --isolate`. `--isolate` gives each test file a fresh process/global so `mock.module` overlays and mounted React roots don't leak across files — the full suite and CI run with it too (RTL auto-cleanup is inert under bun:test, so shared-process runs corrupt co-scheduled component tests; see tests/setup.ts for why the preload must NOT import @testing-library/react). Every test file that renders with RTL must also `import '<rel>/rtl-settle'` — bun wipes the DOM between tests but never unmounts React roots, and React 19 refuses unmounting while a yielded time-sliced render is paused between slices (a state act() cannot join — it resumes via the scheduler's MessageChannel). The rtl-settle afterEach drains the scheduler then unmounts inside act; a test whose final assertion races a response-handling re-render (e.g. asserting a fetch call fired) must end with `await settleReact()` (exported from rtl-settle) — CI's 2-vCPU runners hit these as deterministic failures fast dev machines never reproduce. Integration tests that do REAL HTTP must use `Bun.fetch` — the happy-dom preload replaces global `fetch` with a browser emulation that breaks on real sockets.

Additional mandatory rules:
- **Always clean up:** `afterAll(() => rmSync(testDir, { recursive: true, force: true }))`
- **Mock the logger:** `mock.module('../../src/core/logger', ...)` — prevents noise and avoids side effects
- **Mock the watcher:** `mock.module('../../src/core/watcher', ...)` — prevents chokidar from watching real dirs
- **Mock AppServices/runtime adapters:** Prevents tests from sending real messages to agents
- **Never hardcode `~/.bakin/`** or `process.env.HOME` in test fixtures
- **Use `tests/plugins/test-helpers.ts`** (`activatePlugin`, `callRoute`, `callTool`) for plugin tests — these provide properly isolated mock contexts

If a test does not mock the content-dir resolvers, it **will** eventually write to `~/.bakin/` and corrupt production data. Pure scanner tests under `tests/architecture/` and the external-author-style tests under `tests/sdk-testing/` and `examples/*/tests/` are the only exceptions — the former import no app modules, the latter import only the published `@makinbakin/sdk/testing` harness (whose per-test temp dir IS the isolation); the mock checker hook handles both cases explicitly.

## Key Patterns

- **SSE Broadcasting** — `broadcast()` from `src/core/sse.ts`. Uses `globalThis.__bakinBroadcast` to survive Bun's HMR / module re-evaluation. Two channels: activity (progress) and audit (structured events).
- **Agent Activity** — Agents report progress via `bakin_log_progress` MCP tool → `logProgress()` in task-service → SSE broadcast. Structured audit via `appendAudit()` → `audit.jsonl` + SSE + Antfly.
- **Execution Ledger** — Exactly-once task firing/completion via SQLite coordination facts at `~/.bakin/bakin.db` (`packages/core/src/storage/db.ts` is the SOLE `bun:sqlite` importer — architecture-test enforced; domain verbs only in `packages/core/src/execution/ledger.ts`, app facade `src/core/execution-ledger.ts`). UNIQUE constraints ARE the locks: cron fires claim before task creation, completions are first-write-wins (retries get `alreadyComplete: true`, never an error), every dispatch path claims its run before sending, the watchdog supersedes-first, billed image results are durable idempotency rows (no TTL). Duplicates are suppressed + audited (`*_suppressed` events) and surfaced by the doctor `execution-safety` check; ledger-unavailable **fails closed**. Coordination facts only — never content, never search rows. The server takes a singleton lock (`~/.bakin/server.lock`) before any side effect. Deep reference: `.claude/knowledge/execution-ledger.md`.
- **Dispatch** — Concurrent fire-and-settle dispatch (in-flight turn registry, `settings.dispatch.maxConcurrentTurns`/`maxTurnsPerAgent` caps) with per-attempt provider sessions (`threadId: task:<id>:d<seq>` == the ledger run id; seq is ledger-owned). Failures are typed `RuntimeError`s classified by `kind` — **never by error-message text** (architecture-test enforced). Deleting a task aborts its in-flight turns (`AbortController` per registry entry → `MessageArgs.signal`; kind `'aborted'` settles clean — no recovery ladder), and the watchdog sweeps + force-releases orphaned turns whose task is gone (#604). Tasks can be assigned to a **team** instead of an agent (#189): dispatch resolves the best-suited member before the claim via the `team.resolveAssignment` hook (LLM-routed over an ephemeral runtime turn — no API keys; sticky once resolved; structural failures block honestly, never silently re-route). Workflow steps target teams the same way via the `team:<id>` agent token (#611, sticky per step on the instance). **Same-agent concurrency:** per-agent parallelism is capability-gated — `CapabilitySet.concurrency.sameAgentTurns` (`isolated` on Pi via per-turn `MessageArgs.runWorkspace` cwd; `serialized` on OpenClaw ⇒ clamp to 1 with an audit receipt). Isolated dispatch turns run in core-owned per-run dirs under `~/.bakin/run-workspaces/` (sidecar-classified, watchdog-swept, size-budgeted, doctor `dispatch.run-dirs`); repo-bound tasks (task `repoPath` / `projects.getRepo`, allowlist-gated) get a worktree at `<runDir>/repo` on branch `bakin/run/task-<id>-d<seq>` — the branch is the deliverable, the checkout dies at successful settle. Deep references: `.claude/knowledge/dispatch.md`, `.claude/knowledge/team-aware-assignment.md`, `.claude/knowledge/same-agent-concurrency.md`.
- **Work-Class Model Routing** — ONE routing + spend-attribution key: every LLM-consuming call site is a `WorkClass` (`src/core/model-routing.ts` — 5 dispatch classes classified from task shape + auto-title/enrichment/relay/team-routing/send system classes declared at the call site; `chat` is metered-only). Routes (work class → model/thinking) + tag overrides live in models-plugin `settings.routing`, resolved per turn (dispatch via `resolveDispatchRouting`, system sends via `resolveSystemRoute` in `src/core/system-route.ts`); no route = inherit (agent default). Thinking levels clamp to the runtime's declared `routingSupport().supportedThinkingLevels` (clamp-and-warn with receipt + audit — never a silent drop; conformance-pinned). Every metered turn writes its **route receipt** on `run_costs` (`work_class` REQUIRED on `RunCostInput`, `route_source` = tag/class/inherit) — the dimension that routes IS the dimension spend reports on (`byWorkClass` facet, Spend tab + `bakin spend` tables, Team Diagnostics "via" line). The `models.routing` health check flags unrouted system classes / unavailable-model routes / standing clamps / premium-on-cheap, with a one-click apply-recommended-routes repair (`POST /routing/recommend`, cheapest-available proposals; cheap-vision via `@bakin/core/llm/vision-models`). Never add a parallel routing config — team-routing's model rides the same matrix. Deep reference: `.claude/knowledge/models-plugin.md`.
- **Cost Control (Budget/Spend)** — ONE spend engine (`src/core/budget-spend.ts` `assembleBudgetSpend`: attributed `run_costs` + the unattributed usage.db delta — total-observed basis) behind every surface: the dispatch budget gate, the per-call billed-media gate, the budget health check, `/spend`, `/budget/status`, and `bakin spend/budget` — **never add parallel spend math**. Policy is a rule list (scope global|agent|provider × billing lane; **unit-per-lane**: metered rules cap estimated USD, subscription rules cap tokens — subscription spend is NEVER shown as dollars). Gate order: routing → kill switch (`settings.dispatch.paused`) → budget → claim; breaches open durable `budget_incidents` (ledger UNIQUE = the restart-safe alert debounce) which fan out via `budget-notify` (browser notification + main-agent relay) and resolve via raise/ack/resume; `atCap: 'pause'` holds past window rollover until resolved. Budget-deferred tasks are badged on the board (derived from `/budget/status`, never task metadata). Fresh installs are never silently uncapped (onboarding `budget` component + standing doctor warn). Deep references: `.claude/knowledge/models-plugin.md`, `.claude/specs/cost-control-v2.md`.
- **Session Forensics & Recovery Ladder** — The adapter watches OpenClaw trajectory files (read-only) to fail-fast on session deaths and post-mortem timeouts into `RuntimeTurnError` diagnoses; lost-frame successes are recovered. Diagnosed deaths take a ladder (salvage output as asset → corrective re-dispatch → decomposition into subtasks → diagnostic block), never blind retries. Every dispatch prompt carries OUTPUT DISCIPLINE rules (deliverables → files + `bakin_exec_assets_save`, one at a time; chat stays short). Deep reference: `.claude/knowledge/session-forensics.md`.
- **Usage Recording** — Single in-memory recorder at `src/core/usage.ts`. `recordUsage({ kind: 'mcp'|'rest'|'agent', ... })`; reads via `getUsageFeed`/`getStatsByMs`/`getErrorCount`. **Never add a parallel stat-tracking system** — fragmentation previously broke the health dashboard. Deep reference: `.claude/knowledge/usage-recording.md`.
- **Startup Context** — Per-source measurement + bounds on what a fresh dispatch session costs (#357). Dispatch prompts assemble from labeled sections (measurement path == production path, `src/core/context-report.ts` is the ONE engine behind `bakin agents context`, `GET /api/context-report`, and the warn-only `context.startup-size` doctor check). Workflow prior-step dumps are byte-budgeted (`dispatch.maxWorkflowContextBytes`, visible omission markers); task descriptions are measured but NEVER truncated; static boilerplate is pinned by byte fixtures + a budget test (`tests/fixtures/dispatch-prompts/`). Runtime workspace files are measured read-only via `agents.workspaceFileStats` — names + sizes, never content. Deep reference: `.claude/knowledge/startup-context.md`.
- **Models Cache + Catalog** — Persistent disk cache at `~/.bakin/plugin-settings/models/available.json` plus a curated catalog (`plugins/models/data/known-models.ts`) merged in server-side. **Never fabricate model metadata.** Deep reference: `.claude/knowledge/models-plugin.md`.
- **Explore & Curated Catalog** — The `explore` plugin is the discovery storefront at `/explore`, promoted by the shell-owned **Make Bakin Yours** card rather than a plugin-contributed nav item. ONE embedded zod-validated catalog (`packages/host/src/data/curated-catalog.json`, v2 — agents/plugins/packs with `kind`/`category`/`useCases`/`builtin`) drives both onboarding recommendations (`src/core/onboarding/recommended-*.ts` via `src/core/curated-catalog/`) and Explore browse+install. Installs call the existing host REST endpoints only (plugin consent flow preserved); update/remove/repair stays in Team/Health — Explore relays status (plugin update state from persisted lockfile markers; agent update state is probe-only via `?check=1`). Remote catalog refresh is user-triggered, cached under `~/.bakin/plugin-data/explore/`. Deep reference: `.claude/knowledge/explore-plugin.md`.
- **Capability Packs & Integration Secrets** — ONE readiness engine (`src/core/agent-packages/capability-readiness.ts`: content/bin/key legs + remediation) behind `GET /api/packages/capabilities`, the `capability.<slug>` doctor findings, `bakin check capabilities`, and the runtime hub. Named secrets live in `~/.bakin/secrets.json` (masked `/api/secrets`, Settings → Integrations & Keys, env-first); `src/core/secret-env.ts` injects declared vars + prepends `~/.bakin/bin` to PATH at server boot (never in `createAppServices`) — Pi shells inherit the server env, so one injection serves every runtime. Pending workflow gates ride the `nav-badge-providers` attention slot (badge + toast/OS notification) — approvals are never silent on any runtime. Deep reference: `.claude/knowledge/capability-packs.md`.
- **Adapter Boundary** — Provider code is factory-only from Bakin's perspective. Use `getAppServices().runtime/search/tasks` in core, `ctx.runtime/search/tasks` in plugins/tools. Runtime config is ADAPTER-PRIVATE (no `config` surface on the contract — arch-test enforced); provider needs cross neutral methods (`credentialStatus`, `models.routingPolicy`, `provisionToolAccess`, roster). Deep references: `.claude/knowledge/adapter-architecture.md`, `.claude/knowledge/runtime-capabilities.md`.
- **Runtime Capabilities & Switch** — Every adapter declares a `CapabilitySet` (`capabilities()`: toolCalling/delivery/imageGen/memory/sessions/workspaceFiles, modes native/shimmed/unavailable) + `describeToolAccess()` (in-process Pi / native-MCP OpenClaw); `channels`/`cron` are OPTIONAL members (Pi omits them — consumers feature-detect, `.channels!.`/`.cron!.` banned). ONE renderer (`src/core/tool-access.ts`) feeds dispatch prompts AND the AGENTS.md tool-access section (composed live from the active runtime; runtime switches surface as `tool-access` drift). Tool-access provisioning is adapter-owned (`provisionToolAccess` at boot/install/roster-change — never in `createAppServices`). `switchRuntime` (`src/core/runtime-switch.ts`) orchestrates backup→flip→provision→`reconcileRoster` (model + subagent-model mapping, honest unmapped report)→workspace content carry (`src/core/workspace-carry.ts`: soul/memory verbatim + agent-authored skills via `runtime.skills` for switch-created agents; `--no-copy-workspaces` opts out)→drift-gated sync→capability + can't-carry + target-credential report; `--dry-run` previews the whole report with ZERO writes (read-only secondary target; `initialize()` is write-free by conformance pin — Pi seeds `main` at provision, not initialize). Surfaces: `bakin runtime [use <adapter> [--dry-run] [--no-copy-workspaces]]`, `POST /api/runtime/switch { target, dryRun?, copyWorkspaces? }`, the `/runtime` page. Never hand-edit `settings.runtime.adapter` — it skips every carry/provision step. Completed switches need a server restart (plugins bind `ctx.runtime` at activation). The runtime conformance suite (`tests/integration/runtime-conformance/`) is the ACCEPTANCE GATE for any adapter — shared behavioral checks (messaging/stream/tap/capability-honesty/provisioning/not_found) run against the dev mock, Pi, and the OpenClaw mock, with a teeth file proving the checks bite; the default mock runtime is the MINIMAL shape (no channels/cron — opt in via mockChannels()/mockCron()). Deep reference: `.claude/knowledge/runtime-capabilities.md`.
- **Discord Delivery Bridge (#669)** — Runtime-neutral `ChannelBridge` (`packages/core/src/delivery/bridge.ts`, threaded via `AdapterInitOpts.channelBridge`) with the Discord implementation confined to `src/core/delivery/` (arch-test enforced; `@discordjs/core` stack, no native deps). Pi serves `runtime.channels` by delegation when configured (`delivery: 'shimmed'`; conformance pins shimmed ⇒ surface-present). Boots at server boot ONLY when configured AND the active runtime lacks native delivery (same-token double-handling guard — stop the OpenClaw daemon if the bot answers twice); never inside `createAppServices()`. Config at `settings.integrations.discord` (non-secret, System & Alerts; approver/inbound allowlists FAIL CLOSED); token = secret-store `discord.botToken`. Approval cards carry the approvalId in the embed footer (restart-safe buttons), reject reasons ride a Discord modal, and the durable approval record stays the only authority. Inbound chat (Phase B): bridge-gated @mentions/DMs become real chats via `channels.subscribeInboundMessages` -> `plugins/chat/lib/channel-inbound.ts` (externalKey binding, one turn engine, typing pulse, reply-on-done). `delivery.*` audit events + the `delivery-discord` doctor check keep failures visible; no durable outbox by design (approval rehydration + in-app attention cover outages). Deep reference: `.claude/knowledge/delivery-bridge.md`.
- **OpenClaw Home Directory** — Adapter-private `getOpenClawHome()` / `getOpenClawPath()` in `packages/adapter-openclaw/src/home.ts`. Resolution: `OPENCLAW_HOME` env → `~/.openclaw/`. For dev without OpenClaw: `bun run dev:mock` (Imitation Crab in `dev/imitation-crab/`); reseed with `bun run mock:seed --force`.
- **Dev Rig (dual-runtime)** — `bun run instance up|dev [--mode native|isolated|sandbox] [--runtime openclaw|pi]` (`scripts/instance.ts` + `scripts/instance/*`) runs a REAL runtime against dev-scoped state without touching `~/.openclaw`, `~/.pi`, or any OS service: OpenClaw in Docker, Pi in-process with a throwaway `PI_HOME` (auth via the pi TUI's `/login` — there is no `pi login` subcommand). Adapter selection via the `BAKIN_RUNTIME_ADAPTER` env override (applied at the `getSettings()` cache chokepoint; never persisted). Container-written deliverables save as real assets via `BAKIN_AGENT_PATH_MAP` prefix translation. Isolated instances spawn their own antfly on 3838 (guest-URL settings keep the adapter from ever provisioning the machine-global `io.bakin.antfly` unit — it was hijacked once, 2026-07-11). Deep reference: `.claude/knowledge/dev-rig.md`.
- **Content Directory** — `getContentDir()` in `packages/core/src/content-dir.ts`. Resolution: `BAKIN_HOME` env → `~/.bakin/`. Well-known paths via `getBakinPaths()`.
- **Versioned Assets** — An asset is a stable `assetId` naming a directory of versioned files + one `manifest.json` (no per-file sidecars). Linear versions, free `currentVersion` pointer, exports are derived (not versions). Manifest writes are atomic + serialized per-asset; addressed by id (`/api/assets/<assetId>[/v/<n>|/thumb|/export/<name>]`); one search row per asset; mutations emit `asset.changed`. Image tools (`generate`→v1, `edit(assetId)`→new version) are idempotent to prevent client-timeout double-bills. `bakin_exec_assets_save` upserts by source path. **Enrichment:** on create/new-version an async queue calls a vision LLM and writes caption/OCR/tags/transcript/summary into the manifest (durable, user-editable, never re-billed — `done+forVersion` is the skip guard; user edits are never clobbered); scanned/text-less PDFs render their first 3 pages through the core PDF engine into the same vision pipeline (page-labeled ocrText, #747) instead of skipping; both engines (runtime AND direct-key) meter work-class-`enrichment` spend; search docs include these fields. **Import is explicit:** raw file drops NEVER auto-become assets — the watcher badges unmanaged files, the Assets Import tab / `bakin assets import` / MCP tools adopt them (D7). Deep reference: `.claude/knowledge/assets-versioning.md`.
- **Brands** — The `brands` core plugin (#419): brand records under `~/.bakin/brands/<id>/` (zod manifest for machines — palette/rules/asset refs; markdown for agents — guidelines/lessons). Tasks carry optional `brandId` (mirrors projectId); effective brand resolves LAZILY at dispatch (own → parent ancestry → `projects.getBrand` hook) and injects a byte-budgeted two-tier card (`dispatch.maxBrandContextBytes`, visible omission markers, `brand` section between `project` and `lessons`; full card subagent/main, one-liner triage). Missing/draft brands defer-in-todo pre-claim (budget-defer pattern) with notify-once + derived board badge — never dispatched brandless, never silently. Every branded dispatch writes a `brand.injected` audit record; image tools take `brandId` (palette merge + default references + `generation.{brandId,brandFingerprint}` provenance). Draft lifecycle: builder questionnaire OR website mode (URL-led — the agent mines the sources for palette/voice/terminology) → agent authors via draft-gated write tools → publish; agents NEVER mutate published brand identity (add_lesson is the one live write). Portable repo format imports/exports (`bakin brands import github:…`). UI: "Branding" nav (Paintbrush), path routes `/brands/$brandId` + `/brands/$brandId/docs/$kind/$name` (dedicated doc editor), ONE staged-manifest SaveBar save model, server-computed kit completeness (`lib/completeness.ts`). Deep reference: `.claude/knowledge/brands-plugin.md`.
- **Layered Context & Agent Sync** — Every agent workspace file carries ONE Bakin-managed block composed from layers: `~/.bakin/team/context/global.md` (all agents) + `roles/{orchestrator,subagent}.md` (Bakin-shipped defaults inside their own managed block) + `<teamId>.md` (OrgTeam members) + package template + lessons. Outside the block is agent territory — never touched. `bakin agents sync` (CLI/REST/doctor-repair share one engine) recomposes, re-projects, verifies, and writes a per-agent receipt; changes take effect without restart via `post-sync-reload.ts`. The doctor's `team.agent-sync` check is local-only; upstream fetches are user-initiated. Deep reference: `.claude/knowledge/layered-context.md`.
- **Conversation Kit & Chat** — ONE conversation UI **and ONE turn engine** for every chat-like surface (#703): server-side, `createConversationTurnService` (`src/core/conversation-turns.ts`) runs background turns detached from HTTP (202 on send, 409 busy, incremental persistence via `createTurnRecorder`, consumer-named bus events, abort → clean done + `aborted` row) — chat, brands, and the bits projects/messaging brainstorms all consume it; external plugins via `ctx.conversations` (declarative metering pinned to work class `chat`, run ids force-namespaced, user-plugin event names self-namespaced). Client-side, the SDK conversation kit (`src/components/conversation/` → `@makinbakin/sdk/components`) owns the turn model (`foldConversation` — THE single chunk-folding engine), the bus-driven `useConversationThread` (optimistic echo, mid-turn rehydration), `useConversationAttention` + shared attention rules (badges/toast/chime/OS parity for every surface), renderers (`Conversation`/`AgentTurn`/`ActivityGroup` collapsed-tools→drawer/`Composer` with drafts+history+attachments), the embedded `ConversationPanel`, and server helpers (`conversationThreadId`, `createTurnRecorder`). New surfaces COMPOSE the kit — never hand-roll message/tool rendering (`TurnOutputView` survives only as a thin wrapper for single-turn embeds) and never per-request conversation streaming (deleted in #703). The `chat` core plugin is the kit's reference consumer and the day-to-day hub: transcript schema v2 (structured tool rows, turnId, aborted markers; `~/.bakin/chat/`), one in-flight turn per chat (409) with abort, image + PDF attachments (chat-owned files; images capability-gated + downscaled via `@bakin/core/media`, PDFs/non-raster ride the engine's file-lane path notes to the `bakin_exec_pdf_*` tools backed by the ONE PDF engine `src/core/pdf/` — also the assets extractor's PDF path, #742), budget-gated auto-titles, unread/attention system (nav badge + tab-title prefix + toast/chime/OS notification via the global `nav-badge-providers` slot), draft-mode creation, and transcripts in global search. Turns stream as `chat.chunk`/`chat.done`/`chat.error`/`chat.titled` plugin-events over the shared SSE bus. Deep references: `.claude/knowledge/conversation-kit.md`, `.claude/knowledge/chat-plugin.md`.
- **Plugin Communication** — Cross-plugin and core ↔ plugin calls go exclusively through the HookRegistry (`packages/core/src/hooks/hook-registry.ts`). Plugins register hooks in `activate()` via `ctx.hooks.register(name, handler)`; callers use `getHookRegistry().invoke<R>(name, data)`. Hook naming: `{pluginId}.{operation}`. No direct imports between plugins.
- **MCP Tool Registration** — Fully dynamic. The exec-tool registry is `src/core/exec-tools/registry.ts` (runtime code; moved out of `scripts/lib/` in WS2). Plugins register exec tools via `ctx.registerExecTool()` during activation; the built-in tools (`src/core/exec-tools/tools/*.ts`) self-register via `addExecTool()` on import; `mcp-server.ts` calls `getAllExecTools()` at startup.
- **Plugin Settings** — Each plugin declares a `settingsSchema`; the settings page renders schemas via `PluginSettingsRenderer`. Values persisted at `~/.bakin/plugin-settings/{pluginId}.json`, accessible via `ctx.getSettings<T>()`. The same renderer drives a built-in **System & Alerts** tab that edits `~/.bakin/settings.json` via `/api/settings`. Watchdog re-reads settings every cycle — no restart needed.
- **Server Logging** — `createLogger()` writes JSON lines to stdout AND `~/.bakin/logs/server.log` (10 MB rotation, single backup). Survives `nohup`/`launchd` detached stdio. Disable with `BAKIN_DISABLE_FILE_LOG=1`. Tests skip the file transport via `NODE_ENV=test` / `VITEST`. Every 5xx catch handler logs the stack via `log.error('...', err, { ...context })`.
- **URL State, Deep Linking & Navigation** — Presentation-based taxonomy (spec `.claude/specs/routing-overhaul.md`): path = page identity (lists, full-page details like `/chat/$chatId`, creation surfaces), query = overlays (drawers `?taskId=`/`?jobId=`/`?recordId=` — by design) + tabs + filter/view state via `useQueryState`/`useQueryArrayState` from `@makinbakin/sdk/hooks` (params omitted at default; setters batch per tick; values are plain strings — never JSON-coerced). Internal navigation NEVER full-reloads: `PluginLink`/TanStack `Link`/`useRouter().push` only — enforced by `tests/architecture/no-hard-navigation.test.ts` + mirrored ESLint rules (keep the allowlists in sync). Element-level scroll restoration is on; unknown paths render the shared `NotFoundPage`. Pages must wrap content in `<Suspense>`. Deep reference: `.claude/knowledge/url-state-deep-linking.md`.
- **Search Indexing** — Content types declare `schemaVersion` (required) + capability legs; registration auto-creates the blue/green versioned table, wires the `/search` route, and joins global search. File-backed plugins use `ctx.search.registerFileBackedContentType()` (watcher sync/unlink → the durable outbox; NO boot reconcile — boot does nothing). Non-file-backed plugins call `index()/remove()/transform()` (all journal-first, fire-and-forget safe). Cross-plugin: `GET /api/search` (`?types=` filter); engine-down returns `503 search_unavailable` — UIs show an honest state, never silent fallbacks. Client side: `registerPlugin({ search: { hitRenderers } })` renders hits in the ⌘K overlay. Rebuilds are blue/green (`bakin reindex`, `search.rebuild.*` SSE) — search stays available throughout. Deep references: `.claude/knowledge/search-system.md`, `.claude/knowledge/search-plugin-guide.md`.
- **Memory Observability** — Read-only dashboard over runtime memory tiers + Bakin's audit log, surfaced through the unified `bakin_memory` table with a `tier` facet. Incremental indexing via persisted byte offsets in `~/.bakin/plugin-settings/memory/offsets.json`; stable SHA256 row IDs make upserts idempotent (change-detection is the search outbox's acked-hash — the old boot dedupe cache is gone; `reindex()` delegates to a side-effect-free `enumerateAll()` and FAILS LOUDLY when the runtime is down so blue/green backfills park instead of flipping thin). Schema changes ride the content-type `schemaVersion` (blue/green). **Memory cleanup** (`lib/routes/cleanup.ts` + `lib/cleanup.ts`): find a stale term across tiers → dispatch a cleanup task to each affected agent (the agent edits its own source; Bakin never writes runtime-memory content) → verify. Deep reference: `.claude/knowledge/memory-plugin.md`.
- **Onboarding** — `src/core/onboarding/` — 15 components (the `COMPONENT_ORDER` array; the `budget` component prompts for spending caps — skippable, never a silent default) with shared `check()` + `install()` contract. `bakin install search` downloads the pinned engine AND provisions the OS service (LaunchAgent/systemd user unit; upgrades are stop→swap→restart). CLI: `bakin onboard --yes` for non-interactive setup; `bakin mkdir`, `bakin install {search,search-models,plugin-assets,agent-sync}`, `bakin check {runtime,search,search-models,llm,channels,plugin-assets,agent-sync,capabilities,all}`, `bakin settings init`. Doctor gates on `~/.bakin/.onboarded` marker.
- **Debug Mode** — Global client-side toggle. State in Zustand + localStorage (`bakin-debug`). Access via `useDebug()` from `@makinbakin/sdk/hooks`. Toggle button in header (Bug icon). URL `?debug=true` activates as a one-shot seed.
- **Shared UI Components** — All shared UI lives under `@makinbakin/sdk/components` — plugins always import from there, never from `packages/host/src/components/` directly. `PluginHeader` (title + count + search + actions) and `FacetFilter` (popover multi-select, back with `useQueryArrayState`) are the most reused.
- **Agent Health Diagnostics** — The supervision layer (#385). ONE engine per concern, every surface a thin client: `src/core/agent-burn.ts` (effort/spike heuristics + **provenance buckets** (#691): usage.db rows carry adapter-labeled session origin, so non-task usage splits into `interactive` (external-origin sessions — the operator's own chats, advisory) vs `unexplained` (bakin/unknown-origin minus attributed, watch) vs `runaway` (evidence-backed autonomous accumulation, action_required page with a cron-jobs downgrade guard); NULL-honest, day-aligned, `settings.burn` tunables incl. `runaway*`), `scanAgentSync` (live per-agent scan endpoint + cached doctor fleet state), `context-report.ts`, and `plugins/team/lib/timeline.ts` (run spine + audit interleave). Surfaces: Health Overview incidents + Agents usage/efficiency, live facts, and stacked daily chart; Team Diagnostics + Team Overview chips + `drifted` badge; `bakin agents doctor <id>`. Per-agent Health incidents attach structured `{ kind: 'agent', id }` resources with bounded evidence — UIs never parse message text. Chart kit (`StackedColumnChart`/`BarChart`/`LineChart`/`Sparkline` + exact `ChartDataTable`) is hand-rolled in the SDK — no chart library; the palette slot order is the CVD-safety mechanism, never reorder. Deep reference: `.claude/knowledge/agent-health-diagnostics.md`.
- **Doctor & Canonical Health** — Every diagnostic check is owner-registered through `ctx.registerHealthCheck`; deterministic mutations register separately through `ctx.registerHealthRepairAction`. `src/core/doctor.ts` coordinates full/targeted single-flight execution, while the per-check cache retains honest last-known evidence and projects one immutable SDK-owned `HealthReport` for UI, CLI, HTTP, badge, notification, delegation, and repair consumers. **Sensitivity (#690):** producers stamp incident `class` (10-value SDK enum); the ONE policy in `health-report.ts` projects `effectiveDisposition` under `settings.doctor.sensitivity` (`developer|standard|quiet`, default standard) — standard calms usage_anomaly/cleanup_backlog/policy_denial/unsupported_surface to advisory, unclassified/error-status never demote, quiet badges+escalates only action_required (D10), raw disposition survives on the wire ("Calmed from watch" + class chips on cards), flips republish without restart. Plugin-owned checks live at `plugins/{owner}/lib/health-checks.ts`; system-level checks live under `plugins/health/lib/system-checks/`; adapter checks join the same owner-aware registry at composition. Missing, stale, failed, or invalid evidence is Unknown, never healthy. Stable IDs/resources/resolutions carry identity — never parse copy or reintroduce the retired flat result/inline-fix contract. Deep reference: `.claude/knowledge/doctor-and-health-checks.md`.
- **Scheduled Domain Events (#191)** — Plugins put their own dates (publish dates, milestones, deadlines) on the Schedule calendars via a hook suffix convention: register `{pluginId}.scheduledEvents` (`ScheduledEventsQuery` → `ScheduledDomainEvent[]`, SDK-typed) and optionally `{pluginId}.rescheduleEvent` — the ONE sanctioned mutation; everything else deep-links into the owner. Schedule fans in over all providers (2s budget each, zod at the boundary, pluginId spoof-proofed) and DROPS a broken/slow provider from that response with `droppedProviders` honesty — never a broken calendar. Events ride `GET /occurrences` and render as distinct chips (dashed, `owner · kind`). Tasks is the in-tree provider (`availableAt`/`dueAt`, reschedulable). Deep reference: `.claude/knowledge/scheduled-domain-events.md`.
- **Plugin Hot Reload (linked plugins)** — `bakin plugins link <localPath>` symlinks a developer source tree as a plugin. With `BAKIN_DEV_HOTRELOAD=1`, file saves trigger build + in-process module swap (no restart). Pipeline: chokidar watcher → debounce → per-plugin pipeline mutex → `buildUserPlugin` → sweep registries (hooks/exec-tools/health-checks/etc.) + clear state arrays → cache-bust `import('${dir}/dist/index.js?v=N')` → re-run `activate(ctx)` against the same ctx → bump version + broadcast `dev:plugin:reload`. Browser receives the SSE event, re-fetches `client.js?v=N`, re-runs `registerPlugin`. Safety net: every `/api/plugins/<id>/*` response carries `X-Bakin-Plugin-Version`; client wraps `fetch` and forces reload on drift if an SSE event was missed. Build errors broadcast `dev:plugin:error` (overlay) + `dev:plugin:recover` on next success. `onShutdown` errors never propagate — a buggy shutdown can't brick the dev loop. Cache-bust uses an always-increment counter (NOT the success-only version) so failed reloads can retry against fresh module URLs. Deep reference: `.claude/knowledge/plugin-system.md` § Hot Reload.

## Reference

- **Contributing:** `CONTRIBUTING.md` — Bun setup, build pipeline, dev loop
- **Plugin authoring:** `docs/src/content/docs/extending/plugins/` (Astro docs site)
- **Agent-package authoring:** `docs/src/content/docs/extending/agents/packages.md`
- **Specs:** `.claude/specs/` — detailed specs for each hardening phase
- **Knowledge:** `.claude/knowledge/` — deep dives on every system above
- **Skills:** `.claude/skills/` — reusable Claude Code operations (create-plugin, audit-plugin, add-component)
