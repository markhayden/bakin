/**
 * `@makinbakin/sdk/patterns` — reusable application-aware UI patterns.
 *
 * The entrypoint is intentionally empty until the owned pattern migrations
 * begin. New exports land here directly, never through the legacy barrel.
 */
export {}
