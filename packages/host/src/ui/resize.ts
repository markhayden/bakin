// Host-owned bridge for the legacy Drawer, matching the modal bridge it uses.
// Keep shared resize mechanics and presentation inside the private UI package.
export { ResizeHandle } from '../../../ui/src/behaviors/resize-handle'
export { usePersistedLeadingEdgeResize } from '../../../ui/src/behaviors/use-persisted-leading-edge-resize'
