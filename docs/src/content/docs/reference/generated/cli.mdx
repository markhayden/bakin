---
title: CLI
description: Generated reference for public Bakin CLI commands.
---

import CliCommandCard from '../../../../components/CliCommandCard.astro'

<div class="cli-reference-intro">
  <p>The Bakin CLI is the fastest way to run local setup, check health, manage tasks, install plugins, and script repeatable work. Use it when you want a direct command instead of clicking through the dashboard, or when you need Bakin actions inside shell scripts and automation.</p>
</div>

## Popular

<div class="cli-common-grid">
<a class="cli-common-card" href="#start">
  <code>bakin start</code>
  <span>Start the Bakin server.</span>
</a>
<a class="cli-common-card" href="#onboard">
  <code>bakin onboard</code>
  <span>Run first-time onboarding.</span>
</a>
<a class="cli-common-card" href="#doctor">
  <code>bakin doctor</code>
  <span>Run health checks.</span>
</a>
<a class="cli-common-card" href="#tasks-list">
  <code>bakin tasks list</code>
  <span>List tasks.</span>
</a>
<a class="cli-common-card" href="#tasks-create">
  <code>bakin tasks create &lt;title&gt;</code>
  <span>Create a task.</span>
</a>
<a class="cli-common-card" href="#plugins-install">
  <code>bakin plugins install &lt;source&gt;</code>
  <span>Install a plugin.</span>
</a>
<a class="cli-common-card" href="#search">
  <code>bakin search &lt;query&gt;</code>
  <span>Search indexed content.</span>
</a>
</div>

## Agent Packages

<p class="cli-section-description">Agent package commands install and maintain reusable agent definitions, bundled lessons, prompts, and rules that Bakin manages as local agent state.</p>

<div class="cli-command-list">
<CliCommandCard id="agents-install" name="agents install" summary="Install an agent package." description="Installs or adopts an agent package into Bakin/runtime-managed agent state.">
```sh frame="terminal"
bakin agents install <path|github:user/repo[@ref][#subpath]> [--adopt] [--install-as <id>] [--replace]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;source&gt;</code><span class="cli-command__choices"><span>path</span><span>github:user/repo[@ref][#subpath]</span></span></td><td>choice</td><td>yes</td><td>Choose one of these values.</td></tr>
      <tr><td><code>[--adopt]</code></td><td>option</td><td>no</td><td>Adopt an existing runtime agent.</td></tr>
      <tr><td><code>[--install-as &lt;id&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--replace]</code></td><td>option</td><td>no</td><td>Replace an existing install.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-orphan" name="agents orphan" summary="Orphan an agent package." description="Removes Bakin-managed package state while leaving the runtime agent in OpenClaw.">
```sh frame="terminal"
bakin agents orphan <agent-id> [--keep-blocks] [--force]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;agent-id&gt;</code></td><td>argument</td><td>yes</td><td>Agent id to install, update, remove, or inspect.</td></tr>
      <tr><td><code>[--keep-blocks]</code></td><td>option</td><td>no</td><td>Leave managed blocks on disk.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-delete" name="agents delete" summary="Delete an agent package and runtime agent." description="Removes Bakin-managed package state and deletes the runtime agent through OpenClaw.">
```sh frame="terminal"
bakin agents delete <agent-id> [--keep-blocks] [--force]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;agent-id&gt;</code></td><td>argument</td><td>yes</td><td>Agent id to install, update, remove, or inspect.</td></tr>
      <tr><td><code>[--keep-blocks]</code></td><td>option</td><td>no</td><td>Leave managed blocks on disk.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-remove" name="agents remove" summary="Remove an agent package." description="Compatibility spelling for orphaning an agent package. Use --delete-agent or --delete to also delete the runtime agent.">
```sh frame="terminal"
bakin agents remove <agent-id> [--keep-blocks] [--delete-agent] [--delete] [--force]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;agent-id&gt;</code></td><td>argument</td><td>yes</td><td>Agent id to install, update, remove, or inspect.</td></tr>
      <tr><td><code>[--keep-blocks]</code></td><td>option</td><td>no</td><td>Leave managed blocks on disk.</td></tr>
      <tr><td><code>[--delete-agent]</code></td><td>option</td><td>no</td><td>Delete the runtime agent too.</td></tr>
      <tr><td><code>[--delete]</code></td><td>option</td><td>no</td><td>Delete the runtime agent too.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-lessons" name="agents lessons" summary="Manage agent lesson toggles." description="Lists or toggles lesson blocks for an agent package.">
```sh frame="terminal"
bakin agents lessons <list|enable|disable> ...
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;action&gt;</code><span class="cli-command__choices"><span>list</span><span>enable</span><span>disable</span></span></td><td>choice</td><td>yes</td><td>Lesson action.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="packages-install" name="packages install" summary="Install a package." description="Installs a standalone package.">
```sh frame="terminal"
bakin packages install <path|github:user/repo[@ref][#subpath]> [--install-as <id>] [--replace]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;source&gt;</code><span class="cli-command__choices"><span>path</span><span>github:user/repo[@ref][#subpath]</span></span></td><td>choice</td><td>yes</td><td>Choose one of these values.</td></tr>
      <tr><td><code>[--install-as &lt;id&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--replace]</code></td><td>option</td><td>no</td><td>Replace an existing install.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="packages-list" name="packages list" summary="List packages." description="Lists installed packages. Pass --json for raw API output.">
```sh frame="terminal"
bakin packages list [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="packages-remove" name="packages remove" summary="Remove a package." description="Removes an installed package.">
```sh frame="terminal"
bakin packages remove <package-id> [--force] [--keep-blocks]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;package-id&gt;</code></td><td>argument</td><td>yes</td><td>Package id.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
      <tr><td><code>[--keep-blocks]</code></td><td>option</td><td>no</td><td>Leave managed blocks on disk.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="packages-sync" name="packages sync" summary="Sync a standalone pack." description="Fetches and re-projects an installed skill/workflow/lesson pack.">
```sh frame="terminal"
bakin packages sync <package-id> [--check]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;package-id&gt;</code></td><td>argument</td><td>yes</td><td>Package id.</td></tr>
      <tr><td><code>[--check]</code></td><td>option</td><td>no</td><td>Dry-run; report what would change.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Agents

<p class="cli-section-description">Use these commands to inspect registered agents, review their status and assignments, and send direct messages without opening the dashboard.</p>

<div class="cli-command-list">
<CliCommandCard id="agents-list" name="agents list" summary="List agents." description="Lists runtime agents, or package state when `--packages` is set. Pass --json for raw API output.">
```sh frame="terminal"
bakin agents list [--packages] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--packages]</code></td><td>option</td><td>no</td><td>Show package state instead of the runtime roster.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-status" name="agents status" summary="Get agent status." description="Fetches detailed status for an agent.">
```sh frame="terminal"
bakin agents status <id>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-tasks" name="agents tasks" summary="List tasks assigned to an agent." description="Lists tasks currently assigned to an agent.">
```sh frame="terminal"
bakin agents tasks <id>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-send" name="agents send" summary="Send a message to an agent." description="Sends a message through the running server to an agent.">
```sh frame="terminal"
bakin agents send <id> <message>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>&lt;message&gt;</code></td><td>argument</td><td>yes</td><td>Message text.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Assets

<p class="cli-section-description">Asset maintenance currently focuses on the trash flow: reviewing soft-deleted assets, restoring them, or purging them permanently.</p>

<div class="cli-command-list">
<CliCommandCard id="trash" name="trash" summary="Manage trashed assets." description="Lists, restores, or permanently empties soft-deleted assets.">
```sh frame="terminal"
bakin trash [list|restore|empty] ...
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[action]</code><span class="cli-command__choices"><span>list</span><span>restore</span><span>empty</span></span></td><td>choice</td><td>no</td><td>Trash action.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Diagnostics and Paths

<p class="cli-section-description">Diagnostics commands expose the information needed when something is not behaving as expected: logs, resolved paths, health checks, API docs, and generated agent rules.</p>

<div class="cli-command-list">
<CliCommandCard id="doctor" name="doctor" summary="Run health checks." description="Runs local offline diagnostics by default. Pass --full to include server-backed plugin, task, workflow, search, and runtime checks. Pass --notify-agent with --full to send fresh action-required incidents to the runtime main agent. Pass --fix to plan deterministic repairs or --delegate to create a linked repair task for the main agent; add --yes to execute either repair path. Ack verbs quiet an incident you have seen without hiding it (action_required incidents are snooze-only and re-fire on any evidence change).">
```sh frame="terminal"
bakin doctor [--json] [--full] [--notify-agent] [--fix|--delegate] [--yes] | doctor acks | doctor ack <incidentId> | doctor snooze <incidentId> [--for 24h|7d] | doctor unack <incidentId>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
      <tr><td><code>[--full]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--notify-agent]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--fix]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--delegate]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
      <tr><td><code>value</code><span class="cli-command__choices"><span></span><span></span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>doctor</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>acks</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>value</code><span class="cli-command__choices"><span></span><span></span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>doctor</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>ack</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>&lt;incidentId&gt;</code></td><td>argument</td><td>yes</td><td>Required value.</td></tr>
      <tr><td><code>value</code><span class="cli-command__choices"><span></span><span></span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>doctor</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>snooze</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>&lt;incidentId&gt;</code></td><td>argument</td><td>yes</td><td>Required value.</td></tr>
      <tr><td><code>[value]</code><span class="cli-command__choices"><span>--for 24h</span><span>7d</span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>value</code><span class="cli-command__choices"><span></span><span></span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>doctor</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>unack</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>&lt;incidentId&gt;</code></td><td>argument</td><td>yes</td><td>Required value.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="paths" name="paths" summary="Show content directory paths." description="Prints Bakin runtime paths, optionally for one key. Pass --json for raw API output.">
```sh frame="terminal"
bakin paths [key] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[key]</code></td><td>argument</td><td>no</td><td>Dot-notation settings key.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="logs" name="logs" summary="Tail audit logs." description="Prints audit log entries, optionally filtered by channel or agent. TTY output is a compact live view; --json emits newline-delimited JSON events.">
```sh frame="terminal"
bakin logs [filter] [--json] [--lines <n>] [--no-follow]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[filter]</code></td><td>argument</td><td>no</td><td>Optional log filter.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
      <tr><td><code>[--lines &lt;n&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--no-follow]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="docs" name="docs" summary="Print API documentation from the running server." description="Fetches current API route documentation from `/api/docs`.">
```sh frame="terminal"
bakin docs
```
</CliCommandCard>
</div>

## First-Time Setup

<p class="cli-section-description">These commands create the local directories and baseline settings Bakin needs before normal operation. They are most useful on a fresh machine or when repairing a partially configured install.</p>

<div class="cli-command-list">
<CliCommandCard id="mkdir" name="mkdir" summary="Create the Bakin home directory tree." description="Creates or verifies the `~/.bakin` directory tree. Pass --json for machine-readable output.">
```sh frame="terminal"
bakin mkdir [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="check" name="check" summary="Run onboarding checks." description="Runs one or all first-run readiness checks.">
```sh frame="terminal"
bakin check <runtime|search|search-models|llm|channels|plugin-assets|agent-sync|recommended-plugins|recommended-agents|all>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;value&gt;</code><span class="cli-command__choices"><span>runtime</span><span>search</span><span>search-models</span><span>llm</span><span>channels</span><span>plugin-assets</span><span>agent-sync</span><span>recommended-plugins</span><span>recommended-agents</span><span>all</span></span></td><td>choice</td><td>yes</td><td>Choose one of these values.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="install" name="install" summary="Install onboarding components." description="Installs Bakin dependencies, plugin/agent assets, or official recommended plugins and agents.">
```sh frame="terminal"
bakin install <search|search-models|plugin-assets|agent-sync|recommended-plugins|recommended-agents>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;value&gt;</code><span class="cli-command__choices"><span>search</span><span>search-models</span><span>plugin-assets</span><span>agent-sync</span><span>recommended-plugins</span><span>recommended-agents</span></span></td><td>choice</td><td>yes</td><td>Choose one of these values.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="onboard" name="onboard" summary="Run first-time onboarding." description="Runs the full first-run setup flow. By default it hides raw service logs and shows a structured setup report; use --verbose for underlying logs.">
```sh frame="terminal"
bakin onboard [--check] [--yes] [--json] [--force] [--verbose]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--check]</code></td><td>option</td><td>no</td><td>Dry-run; report what would change.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
      <tr><td><code>[--verbose]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Lifecycle

<p class="cli-section-description">Use these commands to run the local server, check whether it is healthy, restart it after configuration changes, and keep the installed CLI current.</p>

<div class="cli-command-list">
<CliCommandCard id="start" name="start" summary="Start the Bakin server." description="Starts the Bakin HTTP server and dashboard. This is the default command when the compiled binary is launched without arguments.">
```sh frame="terminal"
bakin start
```
</CliCommandCard>
<CliCommandCard id="stop" name="stop" summary="Stop a running Bakin server." description="Finds a running Bakin process and sends SIGTERM.">
```sh frame="terminal"
bakin stop
```
</CliCommandCard>
<CliCommandCard id="restart" name="restart" summary="Restart the Bakin server." description="Restarts Bakin using the available service manager or standalone process behavior.">
```sh frame="terminal"
bakin restart
```
  <p class="cli-command__meta">Aliases: <code>reboot</code></p>
</CliCommandCard>
<CliCommandCard id="status" name="status" summary="Show dispatch and server status." description="Prints local server reachability, dispatch interval, last run, next run, and version information where available.">
```sh frame="terminal"
bakin status
```
</CliCommandCard>
<CliCommandCard id="dev" name="dev" summary="Run the source-tree development loop." description="Runs Bakin in watch-mode development from a source checkout. By default output is compact; use --verbose for raw child-process output and debug logs.">
```sh frame="terminal"
bakin dev [--verbose] [--no-color]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--verbose]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--no-color]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="version" name="version" summary="Print the Bakin version." description="Prints the version embedded in the running CLI/binary.">
```sh frame="terminal"
bakin version
```
  <p class="cli-command__meta">Aliases: <code>--version</code> <code>-v</code></p>
</CliCommandCard>
<CliCommandCard id="update" name="update" summary="Replace the current binary with the latest release." description="Downloads the latest release binary and verifies checksums before replacing the installed executable.">
```sh frame="terminal"
bakin update
```
</CliCommandCard>
<CliCommandCard id="setup-service" name="setup service" summary="Install or remove autostart service integration." description="Installs or removes a macOS LaunchAgent or Linux user systemd service that runs `bakin serve` on login, starts it immediately, and verifies the server responds.">
```sh frame="terminal"
bakin setup service [--uninstall]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--uninstall]</code></td><td>option</td><td>no</td><td>Remove the installed service.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Plugins

<p class="cli-section-description">Plugin commands manage Bakin extensions from the command line, including installing official packages, linking local development plugins, and scaffolding new plugin projects.</p>

<div class="cli-command-list">
<CliCommandCard id="plugins-list" name="plugins list" summary="List installed plugins." description="Lists installed plugins and versions. Pass --check to probe for available upgrades — Whiskit artifact installs poll the release's whiskit-artifacts.json over HTTPS, source installs probe the git remote or local path. Pass --json for raw API output.">
```sh frame="terminal"
bakin plugins list [--check] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--check]</code></td><td>option</td><td>no</td><td>Dry-run; report what would change.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-install" name="plugins install" summary="Install a plugin." description="Installs a plugin from a local path or GitHub source. Append #subpath to install from a monorepo directory, or pin a GitHub install with @ref / --ref. --dev symlinks a local source tree for live development. --yes skips the consent prompt. --force replaces an existing install when used with --dev. Pass --json for raw API output.">
```sh frame="terminal"
bakin plugins install [--dev] <path|github:user/repo[@ref][#subpath]> [--ref <ref>] [--yes] [--force] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--dev]</code></td><td>option</td><td>no</td><td>Install in local development mode.</td></tr>
      <tr><td><code>&lt;source&gt;</code><span class="cli-command__choices"><span>path</span><span>github:user/repo[@ref][#subpath]</span></span></td><td>choice</td><td>yes</td><td>Choose one of these values.</td></tr>
      <tr><td><code>[--ref &lt;ref&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-export" name="plugins export" summary="Export installed user plugins." description="Writes a portable manifest for installed user plugins from ~/.bakin/plugins/lock.json. Without a file, prints JSON to stdout. Pass --json with a file for a machine-readable operation result.">
```sh frame="terminal"
bakin plugins export [file] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[file]</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-import" name="plugins import" summary="Import an exported plugin set." description="Installs every plugin in an exported manifest. GitHub plugins are pinned to the recorded commit SHA when present; linked dev plugins are restored as dev installs when their local source path exists. Pass --json for raw import results.">
```sh frame="terminal"
bakin plugins import <file> [--yes] [--force] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;file&gt;</code></td><td>argument</td><td>yes</td><td>Required value.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-upgrade" name="plugins upgrade" summary="Upgrade a user plugin." description="Upgrades an installed user plugin. Whiskit artifact installs refetch the latest published, checksum-verified artifact (toolchain-free); source installs re-pull from git or the local path and rebuild. Widened permissions require consent; --yes skips the prompt. Pass --json for raw API output.">
```sh frame="terminal"
bakin plugins upgrade <id> [--yes] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-publish" name="plugins publish" summary="Assemble a published plugin artifact." description="Assembles a checksummed Whiskit artifact (tarball + .sha256 + whiskit-artifacts.json index) from a plugin directory. --build compiles the plugin first via the shared system-bun build backend, so producers do not need a separate build step. Re-publishing into the same --out directory carries forward other plugins' index entries, keeping each release a complete catalog. Pass --json for machine-readable output.">
```sh frame="terminal"
bakin plugins publish <pluginDir> --out <dir> [--build] [--base-url <url>] [--platform <p>] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;pluginDir&gt;</code></td><td>argument</td><td>yes</td><td>Required value.</td></tr>
      <tr><td><code>--out</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>&lt;dir&gt;</code></td><td>argument</td><td>yes</td><td>Required value.</td></tr>
      <tr><td><code>[--build]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--base-url &lt;url&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--platform &lt;p&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-remove" name="plugins remove" summary="Remove a plugin." description="Removes an installed non-core plugin. Pass --json for raw API output.">
```sh frame="terminal"
bakin plugins remove <id> [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-restore" name="plugins restore" summary="Restore a removed plugin." description="Lists or restores pre-uninstall snapshots from ~/.bakin/.uninstalled. `--snapshot` accepts a safe timestamp token or tarball filename. Restore refuses to overwrite an existing plugin unless --force is passed.">
```sh frame="terminal"
bakin plugins restore <id> [--snapshot <snapshot>] [--force] [--list]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>[--snapshot &lt;snapshot&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
      <tr><td><code>[--list]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-scaffold" name="plugins scaffold" summary="Create a plugin scaffold." description="Creates a starter plugin source tree. Pass --json for machine-readable output.">
```sh frame="terminal"
bakin plugins scaffold <name> [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;name&gt;</code></td><td>argument</td><td>yes</td><td>Name to create.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-sync-manifest" name="plugins sync-manifest" summary="Regenerate manifest contributes from plugin code." description="Builds the plugin and regenerates contributes.apiRoutes + contributes.execTools from its actual routes and exec tools, so the manifest never drifts from code. Client sections (nav, clientRoutes) are author-maintained. --check reports drift without writing and exits 1 on drift.">
```sh frame="terminal"
bakin plugins sync-manifest [dir] [--check] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[dir]</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>[--check]</code></td><td>option</td><td>no</td><td>Dry-run; report what would change.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-link" name="plugins link" summary="Symlink a local plugin source tree for dev mode." description="Registers a local source tree as a developer-mode plugin via a symlink at ~/.bakin/plugins/&lt;id&gt;/. Used with the hot-reload coordinator. --force overrides id collisions with copied installs or core plugins, but already-linked plugins must be unlinked first. Pass --json for raw API output.">
```sh frame="terminal"
bakin plugins link <localPath> [--force] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;localPath&gt;</code></td><td>argument</td><td>yes</td><td>Local filesystem path.</td></tr>
      <tr><td><code>[--force]</code></td><td>option</td><td>no</td><td>Bypass the normal safety guard.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="plugins-unlink" name="plugins unlink" summary="Remove a linked plugin symlink." description="Removes the dev-mode symlink and lockfile entry. Refuses installed (non-linked) plugins — use `bakin plugins remove` for those. Pass --json for raw API output.">
```sh frame="terminal"
bakin plugins unlink <id> [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Schedule

<p class="cli-section-description">Use the schedule command to list and manage recurring jobs that create tasks automatically on a configured cadence.</p>

<div class="cli-command-list">
<CliCommandCard id="schedule" name="schedule" summary="Manage scheduled jobs." description="Lists, creates, pauses, resumes, removes, triggers, or inspects scheduled jobs.">
```sh frame="terminal"
bakin schedule [list|add|pause|resume|remove|run|runs] ...
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[action]</code><span class="cli-command__choices"><span>list</span><span>add</span><span>pause</span><span>resume</span><span>remove</span><span>run</span><span>runs</span></span></td><td>choice</td><td>no</td><td>Schedule action.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Search

<p class="cli-section-description">Search commands query indexed Bakin content, report index health, and rebuild indexes after adapter or data changes.</p>

<div class="cli-command-list">
<CliCommandCard id="reindex" name="reindex" summary="Reindex content." description="Triggers content indexing through the search adapter.">
```sh frame="terminal"
bakin reindex [--table=<name>] [--rebuild]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--table=&lt;name&gt;]</code></td><td>option</td><td>no</td><td>Optional flag with a value.</td></tr>
      <tr><td><code>[--rebuild]</code></td><td>option</td><td>no</td><td>Drop and rebuild indexes.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="search" name="search" summary="Search indexed content." description="Searches indexed Bakin content.">
```sh frame="terminal"
bakin search <query> [--table=<name>] [--agent=<id>] [--limit=<n>] [--facets=<list>]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;query&gt;</code></td><td>argument</td><td>yes</td><td>Search query.</td></tr>
      <tr><td><code>[--table=&lt;name&gt;]</code></td><td>option</td><td>no</td><td>Optional flag with a value.</td></tr>
      <tr><td><code>[--agent=&lt;id&gt;]</code></td><td>option</td><td>no</td><td>Optional flag with a value.</td></tr>
      <tr><td><code>[--limit=&lt;n&gt;]</code></td><td>option</td><td>no</td><td>Optional flag with a value.</td></tr>
      <tr><td><code>[--facets=&lt;list&gt;]</code></td><td>option</td><td>no</td><td>Optional flag with a value.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="search-stats" name="search:stats" summary="Show search index stats." description="Prints registered search table/index health and counts.">
```sh frame="terminal"
bakin search:stats
```
</CliCommandCard>
</div>

## Settings

<p class="cli-section-description">Settings commands are the scriptable path for reading, changing, and seeding local configuration values.</p>

<div class="cli-command-list">
<CliCommandCard id="settings-get" name="settings get" summary="Read settings." description="Reads all settings or one dot-notation key. Pass --json for raw setting output.">
```sh frame="terminal"
bakin settings get [key] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[key]</code></td><td>argument</td><td>no</td><td>Dot-notation settings key.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="settings-set" name="settings set" summary="Update a setting." description="Updates one setting using dot notation. Pass --json for raw API output.">
```sh frame="terminal"
bakin settings set <key> <value> [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;key&gt;</code></td><td>argument</td><td>yes</td><td>Dot-notation settings key.</td></tr>
      <tr><td><code>&lt;value&gt;</code></td><td>argument</td><td>yes</td><td>Value to write.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="settings-init" name="settings init" summary="Seed default settings." description="Creates default settings if missing. Pass --json for machine-readable output.">
```sh frame="terminal"
bakin settings init [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Task Management

<p class="cli-section-description">Task commands cover the day-to-day board workflow: creating work, changing status, recording notes, expressing dependencies, and sending ready tasks to agents.</p>

<div class="cli-command-list">
<CliCommandCard id="dispatch" name="dispatch" summary="Trigger immediate task dispatch." description="Asks the running Bakin server to run the task dispatch cycle immediately.">
```sh frame="terminal"
bakin dispatch
```
</CliCommandCard>
<CliCommandCard id="tasks-list" name="tasks list" summary="List tasks." description="Lists tasks from the task board, optionally filtered by column.">
```sh frame="terminal"
bakin tasks list [--column=<column>]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--column=&lt;column&gt;]</code></td><td>option</td><td>no</td><td>Optional flag with a value.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="tasks-get" name="tasks get" summary="Get task details." description="Fetches one task by id from the running server.">
```sh frame="terminal"
bakin tasks get <id>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="tasks-create" name="tasks create" summary="Create a task." description="Creates a task and optionally assigns an agent or workflow.">
```sh frame="terminal"
bakin tasks create <title> [agent] [--workflow=<id>] [--no-workflow=<reason>]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;title&gt;</code></td><td>argument</td><td>yes</td><td>Human-readable title.</td></tr>
      <tr><td><code>[agent]</code></td><td>argument</td><td>no</td><td>Agent id to assign or target.</td></tr>
      <tr><td><code>[--workflow=&lt;id&gt;]</code></td><td>option</td><td>no</td><td>Optional flag with a value.</td></tr>
      <tr><td><code>[--no-workflow=&lt;reason&gt;]</code></td><td>option</td><td>no</td><td>Optional flag with a value.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="tasks-move" name="tasks move" summary="Move a task." description="Moves a task to a different board column.">
```sh frame="terminal"
bakin tasks move <id> <column>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>&lt;column&gt;</code></td><td>argument</td><td>yes</td><td>Task board column.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="tasks-log" name="tasks log" summary="Log task progress." description="Adds a progress log entry to a task.">
```sh frame="terminal"
bakin tasks log <id> <message>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>&lt;message&gt;</code></td><td>argument</td><td>yes</td><td>Message text.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="tasks-block" name="tasks block" summary="Block a task." description="Marks a task blocked with a reason.">
```sh frame="terminal"
bakin tasks block <id> <reason>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>&lt;reason&gt;</code></td><td>argument</td><td>yes</td><td>Human-readable reason.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="tasks-depend" name="tasks depend" summary="Register a task dependency." description="Sets a task dependency relationship.">
```sh frame="terminal"
bakin tasks depend <id> <dependsOn>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>&lt;dependsOn&gt;</code></td><td>argument</td><td>yes</td><td>Task id this task depends on.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="tasks-complete" name="tasks complete" summary="Complete a task." description="Marks a task complete with a completion summary.">
```sh frame="terminal"
bakin tasks complete <id> <summary>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>&lt;summary&gt;</code></td><td>argument</td><td>yes</td><td>Completion summary.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Workflows

<p class="cli-section-description">Workflow commands are for guided, multi-step task execution. Use them to discover available flows, start one against a task, advance steps, and submit required inputs.</p>

<div class="cli-command-list">
<CliCommandCard id="workflows-list" name="workflows list" summary="List workflow definitions." description="Lists available workflow definitions.">
```sh frame="terminal"
bakin workflows list
```
</CliCommandCard>
<CliCommandCard id="workflows-start" name="workflows start" summary="Start a workflow." description="Starts a workflow instance for a task.">
```sh frame="terminal"
bakin workflows start <taskId> <workflowId>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;taskId&gt;</code></td><td>argument</td><td>yes</td><td>Task id.</td></tr>
      <tr><td><code>&lt;workflowId&gt;</code></td><td>argument</td><td>yes</td><td>Workflow definition id.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="workflows-step" name="workflows step" summary="Get current workflow step." description="Fetches current workflow step details for a task.">
```sh frame="terminal"
bakin workflows step <taskId>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;taskId&gt;</code></td><td>argument</td><td>yes</td><td>Task id.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="workflows-submit" name="workflows submit" summary="Submit workflow step output." description="Submits JSON output for a workflow step.">
```sh frame="terminal"
bakin workflows submit <taskId> <stepId> <json>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;taskId&gt;</code></td><td>argument</td><td>yes</td><td>Task id.</td></tr>
      <tr><td><code>&lt;stepId&gt;</code></td><td>argument</td><td>yes</td><td>Workflow step id.</td></tr>
      <tr><td><code>&lt;json&gt;</code></td><td>argument</td><td>yes</td><td>JSON payload.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

## Other

<p class="cli-section-description">Commands without a dedicated reference family.</p>

<div class="cli-command-list">
<CliCommandCard id="serve" name="serve" summary="Run the Bakin server in the foreground." description="Foreground server entrypoint for service managers and process supervisors. Humans should use `bakin start`.">
```sh frame="terminal"
bakin serve
```
</CliCommandCard>
<CliCommandCard id="runtime" name="runtime" summary="Show the runtime capability report or switch runtime adapters." description="Without arguments, prints the active runtime adapter and its capability report (native/shimmed/unavailable per capability, tool-access status). `bakin runtime use &lt;adapter&gt;` runs the orchestrated switch: settings backup, tool-access deprovision/provision, roster carry-over with model + subagent-model mapping and an honest unmapped report, workspace content carry for switch-created agents (soul/memory verbatim, agent-authored skills to the target's skill location; `--no-copy-workspaces` opts out), honest stays-behind lines (channels, runtime crons, session context, provider config) plus the target's credential status, and drift-gated agent re-projection. `--dry-run` previews the entire report with zero writes. A completed switch requires `bakin restart` so plugins rebind.">
```sh frame="terminal"
bakin runtime [use <openclaw|pi>] [--dry-run] [--no-copy-workspaces]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[value]</code><span class="cli-command__choices"><span>use &lt;openclaw</span><span>pi&gt;</span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>[--dry-run]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--no-copy-workspaces]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-context" name="agents context" summary="Startup-context size diagnostics." description="Reports per-source startup-context sizes for a fresh task dispatch: static prompt sections (measured from the real builders), configured caps for dynamic blocks, runtime workspace file sizes, and observed dispatch-run tokens from the cost ledger. Names and numbers only — never prompt content. Distinct from `bakin diagnostics startup`, which reports server boot timing.">
```sh frame="terminal"
bakin agents context [agent-id] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[agent-id]</code></td><td>argument</td><td>no</td><td>Agent id to install, update, remove, or inspect.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-doctor" name="agents doctor" summary="Combined per-agent health report." description="One view of an agent's health: live drift scan findings, startup-context budget summary, token-burn flags (effort vs outcome, interactive vs unexplained usage, runaway), and the recent activity timeline. A thin client over the same endpoints as the web Diagnostics tab; each section degrades independently when an endpoint is unavailable.">
```sh frame="terminal"
bakin agents doctor <agent-id> [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;agent-id&gt;</code></td><td>argument</td><td>yes</td><td>Agent id to install, update, remove, or inspect.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="agents-sync" name="agents sync" summary="Sync agents with their packages + context layers." description="Fetches package updates, recomposes managed blocks (global/role/team/package/lessons), re-projects skills and assets, verifies, and writes a receipt. --check reports without writing. --reclaim discards local edits on a sentineled skill/asset after confirmation.">
```sh frame="terminal"
bakin agents sync [agent-id] [--check] [--reclaim <target>] [--reclaim-all] [--yes]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[agent-id]</code></td><td>argument</td><td>no</td><td>Agent id to install, update, remove, or inspect.</td></tr>
      <tr><td><code>[--check]</code></td><td>option</td><td>no</td><td>Dry-run; report what would change.</td></tr>
      <tr><td><code>[--reclaim &lt;target&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--reclaim-all]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="skills-install" name="skills install" summary="Install a skill from a hub." description="Installs an Agent-Skills bundle from ClawHub, GitHub, or a local path onto the active runtime. Accepts pasted clawhub.ai/github.com URLs or clawhub:/github: refs. Shows a trust preview (files, translated requirements, hub verdict, security warnings) and requires consent; hub-flagged malicious skills are refused with no override. Re-running install on the same ref re-checks and re-pins — that is the update path.">
```sh frame="terminal"
bakin skills install <url-or-ref> [--yes]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;url-or-ref&gt;</code></td><td>argument</td><td>yes</td><td>Required value.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="skills-list" name="skills list" summary="List managed and unmanaged skills." description="Lists Bakin-managed skills (hub-installed and pack-installed) with version and source, plus unmanaged skills discovered in the active runtime.">
```sh frame="terminal"
bakin skills list [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="skills-remove" name="skills remove" summary="Remove an installed skill." description="Uninstalls a managed skill by its bare name (unprojects from the runtime, updates the lockfile).">
```sh frame="terminal"
bakin skills remove <name>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;name&gt;</code></td><td>argument</td><td>yes</td><td>Name to create.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="skills-map" name="skills map" summary="Map unrecognized skill requirements via an agent." description="Dispatches an agent to read an installed hub skill and propose a requirements mapping (keys, binaries). The proposal is mechanically verified (only literally-present names survive; secret slots are core-minted) and shown as a diff for approval before the manifest is amended.">
```sh frame="terminal"
bakin skills map <name> [--yes]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;name&gt;</code></td><td>argument</td><td>yes</td><td>Name to create.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="diagnostics-startup" name="diagnostics startup" summary="Manage startup diagnostics." description="Reads or updates the local startup diagnostics setting in ~/.bakin/settings.json. This does not require the server to be running; changes apply on the next server start. Environment variables still override it for one-off runs.">
```sh frame="terminal"
bakin diagnostics startup <status|on|off> [--slow-ms <ms>] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;value&gt;</code><span class="cli-command__choices"><span>status</span><span>on</span><span>off</span></span></td><td>choice</td><td>yes</td><td>Choose one of these values.</td></tr>
      <tr><td><code>[--slow-ms &lt;ms&gt;]</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="spend" name="spend" summary="Estimated agent spend + budget utilization." description="Lane-split spend (metered dollars vs subscription tokens — plan quota carries no dollar figure), per-rule budget utilization, on-pace projections, unattributed share, per-agent/per-model rollups, and the kill-switch state. Same engine the dispatch gate enforces, so the numbers match the Spend tab.">
```sh frame="terminal"
bakin spend [--window 24h|7d|30d|all] [--json]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[value]</code><span class="cli-command__choices"><span>--window 24h</span><span>7d</span><span>30d</span><span>all</span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>[--json]</code></td><td>option</td><td>no</td><td>Emit machine-readable output.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="budget" name="budget" summary="Manage spending caps + the dispatch kill switch." description="Shows or edits cap rules (scope global|agent|provider × lane metered|subscription; metered caps are whole USD, subscription caps are tokens), pauses/resumes ALL dispatch (kill switch), and lists/resolves budget incidents (raise a cap &amp; resume, acknowledge, or resume a pause-mode hold).">
```sh frame="terminal"
bakin budget [show|set|rm|pause|resume|incidents] ... (set: --scope global|agent|provider|model --lane metered|subscription; token caps accept k/M suffixes)
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[value]</code><span class="cli-command__choices"><span>show</span><span>set</span><span>rm</span><span>pause</span><span>resume</span><span>incidents</span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>(set:</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>--scope</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>value</code><span class="cli-command__choices"><span>global</span><span>agent</span><span>provider</span><span>model</span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>--lane</code></td><td>option</td><td>no</td><td>Optional flag.</td></tr>
      <tr><td><code>value</code><span class="cli-command__choices"><span>metered</span><span>subscription;</span></span></td><td>choice</td><td>no</td><td>Choose one of these values.</td></tr>
      <tr><td><code>token</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>caps</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>accept</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>k/M</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
      <tr><td><code>suffixes)</code></td><td>argument</td><td>no</td><td>Optional value.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="search-reset" name="search:reset" summary="Reset the search engine clean." description="Stops the engine, wipes its derived index data (content and models untouched), starts it clean, and repair-reindexes every table from source. The recovery verb for a corrupted or wedged engine that survives restarts.">
```sh frame="terminal"
bakin search:reset [--yes]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="brands-list" name="brands list" summary="List brand records." description="Lists all brands (drafts flagged) plus honestly-surfaced invalid records.">
```sh frame="terminal"
bakin brands list
```
</CliCommandCard>
<CliCommandCard id="brands-get" name="brands get" summary="Show one brand." description="Palette, rules, asset groups, guideline/lesson docs, provenance, and the content fingerprint for a brand.">
```sh frame="terminal"
bakin brands get <id>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="brands-import" name="brands import" summary="Import a portable brand." description="Previews (name, palette, counts), confirms, then imports: referenced files become managed assets, path refs rewrite to assetIds, provenance is stamped. Existing ids are replaced only after confirmation — local edits win otherwise. Private repos use your ambient git credentials.">
```sh frame="terminal"
bakin brands import <github:user/repo[/path]|dir> [--yes]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;value&gt;</code><span class="cli-command__choices"><span>github:user/repo[/path]</span><span>dir</span></span></td><td>choice</td><td>yes</td><td>Choose one of these values.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="brands-check" name="brands check" summary="Drift-check an imported brand." description="Compares an INSTALLED brand's provenance commit against its upstream repo. Read-only; refreshing is an explicit re-import.">
```sh frame="terminal"
bakin brands check <id>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="brands-export" name="brands export" summary="Export a brand to a portable directory." description="Writes the portable layout (brand.json with relative file refs, guidelines/, lessons/, assets/) — pushable to a repo and importable by any Bakin.">
```sh frame="terminal"
bakin brands export <id> <dir>
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>&lt;dir&gt;</code></td><td>argument</td><td>yes</td><td>Required value.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
<CliCommandCard id="brands-remove" name="brands remove" summary="Delete a brand." description="Deletes the brand directory after a linked-task warning (pending tasks referencing it defer until the brand exists again). Assets stay in the asset store.">
```sh frame="terminal"
bakin brands remove <id> [--yes]
```
  <table class="cli-command__args">
    <thead><tr><th>Part</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead>
    <tbody>
      <tr><td><code>&lt;id&gt;</code></td><td>argument</td><td>yes</td><td>Resource identifier.</td></tr>
      <tr><td><code>[--yes]</code></td><td>option</td><td>no</td><td>Accept all defaults non-interactively.</td></tr>
    </tbody>
  </table>
</CliCommandCard>
</div>

<aside class="generated-page-note" aria-label="Generated page metadata">
  <span>Generated Jul 31, 2026 · Bakin 0.0.0-dev</span>
</aside>
